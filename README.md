# Annotation Search

AnnoSearch is a command-line tool and server for managing and searching IIIF (International Image Interoperability Framework) and W3C web annotation collections. It provides commands to initialize, load, delete, and search indexes, as well as to start an Express server for handling search requests.

